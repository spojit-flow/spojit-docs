---
template: overrides/main.html
---

___
#### Sum
```
sum(array $value):mixed
```
  Sums all value in given array and returns a number.
```
  sum([1, 2, 3]) => 6
  sum([1.2, 2.2]) => 3.4
  sum([]) => 0
```
